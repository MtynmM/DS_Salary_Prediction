# تحلیل و پیش‌بینی حقوق متخصصان داده (Data Science Salary Prediction)

این پروژه با هدف تحلیل عوامل موثر بر حقوق متخصصان حوزه داده (Data Science, Data Engineering, ML) و ایجاد یک مدل **رگرسیون خطی (Linear Regression)** برای پیش‌بینی حقوق بر اساس ویژگی‌های شغلی و جغرافیایی انجام شده است.

## 📊 مرور کلی پروژه (Project Overview)

در این پروژه، از مجموعه داده `ds_salaries.csv` استفاده شده است تا به سوالات زیر پاسخ داده شود:
1. چه عواملی بیشترین تاثیر را بر میزان حقوق دارند؟
2. آیا موقعیت جغرافیایی (به‌ویژه کار در آمریکا) تاثیر معناداری دارد؟
3. میزان تجربه و سطح ارشدیت چگونه با حقوق رابطه دارد؟

## 🛠️ مراحل انجام کار (Methodology)

### ۱. پیش‌پردازش داده‌ها (Preprocessing)
* **پاکسازی داده‌ها:** بررسی مقادیر گم‌شده و اصلاح فرمت‌ها.
* **مهندسی ویژگی‌ها (Feature Engineering):**
    * تبدیل متغیرهای طبقه‌بندی شده (Categorical) مانند `job_title` به دسته‌های کلی‌تر (مانند ML Engineer, Data Scientist, Manager).
    * تبدیل متغیرهای رتبه‌ای (Ordinal) مانند `experience_level` و `company_size` به مقادیر عددی جهت استفاده در رگرسیون.
    * ایجاد متغیر باینری `is_US_company` برای بررسی تاثیر خاص بازار کار آمریکا.

### ۲. مدل‌سازی (Modeling)
از الگوریتم **Linear Regression** برای ایجاد مدل پایه استفاده شد تا رابطه خطی بین متغیرها و حقوق (`salary_in_usd`) سنجیده شود.

## 📈 نتایج و ارزیابی مدل (Results & Evaluation)

خروجی مدل بر روی داده‌های تست به شرح زیر است:

| معیار (Metric) | مقدار (Value) | توضیحات |
| :--- | :--- | :--- |
| **R-squared ($R^2$)** | **0.500** | مدل توانسته است ۵۰٪ از واریانس حقوق‌ها را توضیح دهد. |
| **MAE** | **$31,403** | میانگین خطای مطلق مدل در پیش‌بینی هر رکورد. |
| **RMSE** | **$43,793** | ریشه میانگین مربعات خطا (حساس‌تر به داده‌های پرت). |

### تحلیل ضرایب (Coefficients Interpretation)
مهم‌ترین عوامل تاثیرگذار بر حقوق طبق مدل عبارتند از:

1.  **موقعیت جغرافیایی (US vs Non-US):** شرکت‌های آمریکایی به طور متوسط **۶۳,۳۵۰ دلار** بیشتر از سایر نقاط جهان پرداخت می‌کنند (بیشترین تاثیر مثبت).
2.  **نقش شغلی (Job Role):**
    * مهندسان یادگیری ماشین (ML Engineers): **+۴۵,۵۶۰ دلار**
    * مدیران و رهبران تیم (Managers/Leads): **+۴۴,۰۰۵ دلار**
3.  **سطح تجربه (Experience Level):** با هر پله ارتقای سطح تجربه (مثلاً از Junior به Senior)، حقوق حدود **۳۰,۷۶۳ دلار** افزایش می‌یابد.

## 💡 بحث و تحلیل (Discussion)

### چرا $R^2$ برابر با ۰.۵ است؟
مقدار $R^2$ نشان می‌دهد که نیمی از تغییرات حقوق توسط متغیرهای موجود در این دیتاست (تجربه، مکان، عنوان شغلی) قابل توضیح است. نیمی دیگر از واریانس (Unexplained Variance) ناشی از عواملی است که در این مدل خطی لحاظ نشده‌اند:

1.  **عوامل کیفی ناموجود:** عواملی مثل "مهارت‌های مذاکره فردی"، "تکنولوژی‌های خاص (Specific Tech Stack)"، "پاداش‌های سهام (Stock Options)" و "سوددهی شرکت" در دیتاست وجود ندارند اما تاثیر زیادی بر حقوق نهایی دارند.
2.  **پیچیدگی غیرخطی:** رگرسیون خطی فرض می‌کند که رشد حقوق همواره ثابت است. در دنیای واقعی، جهش‌های حقوقی ممکن است نمایی باشند یا در سطوح بالا اشباع شوند.
3.  **تفاوت RMSE و MAE:** فاصله قابل توجه بین RMSE و MAE نشان‌دهنده وجود **داده‌های پرت (Outliers)** است (حقوق‌های بسیار بالا در برخی شرکت‌های خاص تکنولوژی) که پیش‌بینی دقیق آن‌ها برای یک مدل خطی دشوار است.

**نتیجه‌گیری:** مدل فعلی برای شناسایی **روندها (Trends)** و **تاثیر فاکتورهای کلیدی** کاملاً معتبر و قدرتمند است، اما برای پیش‌بینی دقیق حقوق تک‌تک افراد (Point Prediction) نیاز به داده‌های جزئی‌تر دارد.

## 🚀 کارهای آینده (Future Work)
برای بهبود عملکرد مدل در نسخه‌های بعدی، پیشنهادات زیر مطرح می‌شود:
* استفاده از مدل‌های غیرخطی و مبتنی بر درخت تصمیم (مانند **Random Forest** یا **XGBoost**) برای کشف روابط پیچیده‌تر.
* جمع‌آوری داده‌های بیشتر در مورد مهارت‌های فنی (مانند Python, SQL, Cloud) به عنوان فیچرهای جدید.
* استفاده از تبدیل لگاریتمی (Log Transformation) روی متغیر هدف برای کاهش اثر داده‌های پرت.

## 💻 نحوه اجرا (How to Run)
```bash
pip install pandas scikit-learn matplotlib
python main.py
